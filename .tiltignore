# Tiltignore - Files to exclude from Tilt's file watching

# Version control
.git/
.gitignore

# Documentation
*.md
docs/
.design/
_research/

# CI/CD
.github/
*.yaml.bak
cloudbuild.yaml

# IDEs and editors
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Test and coverage files
*_test.go
*.test
*.cover
coverage.html
coverage.out

# Build artifacts (outside of container)
bin/
dist/
*.exe

# Dependencies (will be in container)
vendor/

# Temporary files
tmp/
temp/
*.tmp

# Configuration files that shouldn't trigger rebuilds
tilt_config.json
.env.local